{
    "build": {
      "env": {
        "VERCEL_FORCE_NO_BUILD_CACHE": "1"
      }
    },
    "installCommand": "pnpm install",
    "buildCommand": "pnpm build",
    "devCommand": "pnpm dev",
    "outputDirectory": "dist"
  }